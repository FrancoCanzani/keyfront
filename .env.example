# Gateway
ADDR=:8080

# Postgres — local (dev). Swap for PlanetScale at deploy.
DATABASE_URL=postgres://localhost/api_gateway

# Redis — local (dev). Self-hosted on Hetzner at deploy.
REDIS_URL=redis://localhost:6379
